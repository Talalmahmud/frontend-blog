import React from "react";
import ProfileLayout from "../components/ProfileLayout";

const Profile = () => {
    return (
        <div>
            <ProfileLayout />
        </div>
    );
};

export default Profile;
